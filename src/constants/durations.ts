/**
 * Centralized duration constants for consistent timing across the application
 * All values are in milliseconds
 */

// Card interaction timings
export const ADVANCE_DELAY_MS = 800; // Delay before advancing to next card after correct answer
